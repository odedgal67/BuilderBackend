repos:
  - repo: https://github.com/psf/black
    rev: "23.1.0"
    hooks:
      - id: black
  - repo: local
    hooks:
      - id: Unit Tests
        name: Unit Tests
        language: system
        description: runs all unittests in /Tests/unit
        pass_filenames: false
        verbose: true
        entry: python -m unittest discover -s Tests/unit
